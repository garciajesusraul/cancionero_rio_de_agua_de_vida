---
description: Guardar esta charla en texto simple para llevarla a la otra PC
---
Guardá esta conversación actual en la carpeta docs/sesiones/ del proyecto (creala si no existe).

Reglas:
- SOLO texto: lo que dijo el usuario y lo que respondiste, en orden.
- NO copies archivos del proyecto ni salidas técnicas pesadas. Si algo técnico importa, resumilo en 1 línea.
- Nombre del archivo: fecha-hora + tema corto + 6 últimos caracteres del ID de sesión actual, extensión .md. Si ya existe un archivo de esta sesión (termina igual), actualizalo en vez de duplicar.
- Al inicio del archivo poné fecha y una línea "Guardado con /guardar-sesion".
- Al final avisame en simple que quedó guardado y que con el push viaja a la otra PC.
